# Changelog

## Unreleased

- Round 6 of the all-packages test (23 September 2026): twenty-five confirmed findings. Round 5's own fix for a record stored as an object value contradicted itself, so such an export had no valid Representation Map at all: unlisted it failed a mandatory Test, and listed it raised SystemExit before a single Statement was decided. Three further crashes wrote no report: an identity that is a list reaching the Integrity or Lifecycle legs, a Lifecycle carrying two identifiers, and an erasure field holding a list. A terminal State written as one string was iterated character by character, so the leg reported that none is left; the ownership Invariant discarded violations it had already found when a later map row was missing; an Entity with no identity resolved to an Ownership record with no owned object and Passed "each resolving to the one Ownership record that names it". The suite did not finish a 5,000-entity export in fifteen minutes because the ownership leg walked every identity once per record; the Resolver caches what it derives and the same export now runs in half a second. Dispositioning every Statement Descriptive left every check green and made the suite report Core Conformance established over four Tests: a Disposition's Note must name the governance record that decided it, the Note may change only with the Disposition, and the report and the run both state the size of the measured set. The Reviewer Record binds six inputs rather than four, since the Alias File and the Test Catalogue decide which Statements exist and which procedure each one gets; its binding line is read wherever a Markdown author would write it, and a line the tool cannot read stops the run instead of being treated as no line; an unbalanced fence stops it too. Round 5's three redirect guards, its three Deleted-state legs and its two identity-scope legs were covered by no test at all, and all 192 tests passed with each of them deleted: the site fixture can redirect now, and each leg has a test that fails without it. `site_error_hunt.py` reads `/.well-known/ocom.json`, which discovery.json calls the primary discovery point and nothing had ever opened. `AO-095` recorded (a Memory Record in the Deleted state must drop attributes `Memory Record.md` makes mandatory). `Release-Readiness.md` said the site's reading path stays at the v1.3.0 compilation; it serves a projection of a commit in no Release. `ROADMAP.md`, published at `/evolution`, still listed the Evidence Register among the records the site holds. 203 tests.
- Round 5 of the all-packages test (23 September 2026): twenty-nine confirmed findings, six decisive, two of them regressions introduced by round 4 the same day. The live `publication_health.py --check` was failing 39 rows because round 4 asserted HTML for `/explain/<id>`, which the site serves as JSON and the fixture served as HTML; the fixture now serves what the site serves, every presence row carries the shape its own rule names, and each machine entry point is read as the media type `discovery.json` declares for it. Round 4's own regression test for `register_counts.py` passed with the tool removed, because the test fixture never carried `README.md`; the fixture carries it, a fixture edit that changes nothing is now an error, and the tool checks each document for the registers it is expected to state, reading the body rather than the revision history. In `validate.py`: a record stored as a plain object value, and a collection listed under a type that binds no identity, were in neither identity Test; the "not a list of records" refusal skipped every nested collection the map states; an Object with two identities, a Workflow whose steps are references, and an erasure field holding a list each crashed the run and wrote no report; two spellings of one declared system were two namespaces; the Lifecycle index was keyed by a bare identity, so a second declared scope masked a Lifecycle violation; the erasure laundering path was reopened by nesting the content inside a preserved field, by prefixing it with a timestamp, and by pointing the map's creator row at it, so an Integrity Test that excluded any record is now pending rather than Pass; a comment span could delete judgment rows silently; a Reviewer Record could declare bindings nothing verified and carried its judgments across a rewritten register, so it declares four digests including the Requirement Register; a `Review` disposition was decided mechanically and the reviewer's judgment discarded; and the extension attestations are five fields whose value is `yes`, after two attempts to read them out of a sentence. `published_source_parity.py` refuses a redirect at a published path. `AO-094` recorded (an erasure record grants an exclusion and is itself in no Test); `AO-083` carries a postscript naming five machine-facing files the site serves and this repository does not source; `CAND-023`'s Next Action and the Evidence Register's own revision row corrected; `Adoption/Reference Serialization.md` said the map has three kinds of row and called the integrity method a declaration, which silently turned off every Integrity Test for a map written to it. 192 tests.
- Round 4 of the all-packages test (23 September 2026): twenty-two confirmed findings, three of them decisive, most again inside the previous round's fixes, each closed with a negative test. In `validate.py`: the last literal-key read in the engines turned three Lifecycle violations into a Pass; a Lifecycle with two initial States crashed the run and wrote no report; a Fail the Domain leg had already found was discarded as pending when a map row was missing; an erasure record that granted no exclusion still switched off the content-address check, turning eight Integrity Tests from pending to Pass; the Deleted-state check accepted any non-empty value as a creation time, so a map row could relabel the surviving content; a nested reference under `id` was never resolved; a judgment row mentioning `<!--` was silently discarded and an unterminated comment hid every row below it; Chapter 8's clause about claiming conformance was left pending in every run and was open to a reviewer's judgment; a collection of records was invisible if its list held one non-record member, if it was a list of lists, if it was an object keyed by identity, or if it sat under a plain object; a subject the map lists nowhere went pending where the same absence stated in words Fails; half an accountable-Ownership-Type declaration switched CAND-025's counting off; and the four extension attestations were satisfied by a sentence that denies all four. The example map lists the Attributes the export nests inside its Entities, and an element a Statement itself calls optional is no longer required: 185 mandatory, 48 Pass, 0 Fail, 3 Review Pass, 134 pending, 3 Not Applicable. `requirement_register.py` no longer compares the Alias File against the commit under test, and CI passes the push's own before-SHA. The registers check is `tools/governance/register_counts.py`, a tool with negative tests, rather than an inline CI script that passed on a wording it could not read. `publication_health.py` asks what a projection's body is rather than only that something answered 200, and does not count a redirect as the file; `site_error_hunt.py` reads `srcset`, unquoted attributes, protocol-relative URLs on its own host and the references inside a stylesheet. `AO-087` said the requirement set takes no Statement from `Lifecycles/`; it takes eight, five of them mandatory. 176 tests.
- Round 3 of the all-packages test (23 September 2026): twenty-one confirmed findings over eighteen distinct defects, most of them inside round 2's own fixes, each closed with a negative test. In `validate.py`: the owner leg could be switched off by not listing an Ownership collection at all; the Transition and Workflow engines still read the literal keys `id`, `name`, `from`, `to` and `entity`, so seven map rows could be deleted with no verdict changing; the primary-governance Invariant reported that every Entity names one Domain over an export in which none did; a collection of records nested inside a record was outside every identity Test; the cross-namespace guard compared scopes and dropped the system, so two External System identities resolved to each other's Ownership record; the erasure exclusion was keyed by the namespace of the erasure record rather than of the record it names, and was granted to a record that still carried its content; a Reviewer Record declaring one of the three digests bound its judgments to that alone; a judgment row inside a fenced block or an HTML comment was read as a judgment; and both Declaration Tests on extensions passed on any non-empty value, including `none`. `CAND-025`'s accountable Ownership Type is now read from the map (`Ownership.type`, `Ownership.accountable type`) and counted, and where the map declares neither the Test says so. The Alias File's append-only rule is enforced against the file's own history (`requirement_register.py --check-aliases [--against <rev>]`, with `fetch-depth: 0` in CI). `publication_health.py` holds every figure to the threshold the record publishes for it and no longer counts a comparison it did not make on `/resolve`. `Conformance-Test-Suite.md` states the Representation Map rows that declare an erasure record and what the exclusion requires, and names the `Extension attestations` field; `Core/Terminology.md` carries Evidence as an entry, which its revision history had claimed since 21 September. The example report reads 185 mandatory, 47 Pass, 0 Fail, 3 Review Pass, 135 pending, 3 Not Applicable. 159 tests.

- Round 2 of the all-packages test (23 September 2026): thirty-four confirmed findings, most of them in the fixes round 1 had made a day earlier, each closed with a negative test. The suite no longer passes a Statement that is a stem with no list, an Invariant that compared nothing, an Integrity Test over records the export declared erased outside the Memory tier, an erasure record naming itself through a map row, a Workflow Statement decided from Events, or an ownership leg resolving across declared scopes; `subject_of` reads both ends of a subject phrase and refuses a process phrase; a declared system splits a namespace only under External System; the Declaration Test sees a collection the map does not list. The Reviewer Record is read as a whole file (one blank line disabled every guard), may declare the inputs it was recorded against, and keeps the mechanical reason it replaced; the report names the checkout it read and digests all six inputs. `requirement_register.py` reads a closed Disposition grammar and refuses a supersession that names nothing. The CI commit check reads every Commit cell and refuses to pass on none, and a new step compares the three records that count the registers against the registers. `publication_health.py` compares rule text, missing lists and the parity blocks field by field, with a new row comparing what `/observatory` and `/resolve` print against the records they render; `site_error_hunt.py` sniffs the body rather than trusting Content-Type, reads every canonical link, honours `X-Robots-Tag` and `none`, and checks where an off-site redirect goes. 138 tests.
- The all-packages test of 22 September 2026: thirty confirmed findings, each reproduced by an independent verifier before it was believed. Fixed in the suite (eleven fail-opens in `validate.py`, from identity resolution through the erasure gate to Reference Integrity), in the governance records (four counting Statements, not eleven; a Census that adds up), in the site tools (`publication_health.py` harvested no identifier from any page and asserted a parity it never computed; `site_error_hunt.py` counted a duplicated sitemap URL twice and trusted an off-site redirect) and in `reference_schema.py` (the second leg of `--check` could not fail). `publication/llms.txt` attributed v1.3.0's Zenodo DOI and Software Heritage SWHID to v1.4.0 on the live site; the Evidence Register gained its v1.4.0 rows. 126 tests.
- `CAND-029` to `CAND-034` filed, each Open with a proposed Decision or disposition: the Memory tier measured through Profile Conformance; a scope as what an identity's assigner guarantees uniqueness within; merge and split as Identity Resolution; no precedence between Policies and Exceptions; the kinds of Owner; temporal boundaries not required for v1.0. `CAND-008` carries a proposed disposition.

## v1.4.0 (22 September 2026)

- Release cut from `main` on 22 September 2026 per `Release-Workflow.md`: Constitution 1.0.1, Core Vocabulary 0.1 (13 governed terms), Specification reading path 1.0, unchanged tracks; the Manifest entry recorded before tagging and completed after publication with the commit, the timestamps and the Zenodo version DOI. The first Release to carry the Memory tier's Evidence definition, the integrity guarantee and the erasure rules, the Package 3 Decisions (`CAND-025` to `CAND-028`), and the Conformance Test Suite tooling with its example run.
- `CAND-028` (Decided 22 September 2026): `docs/Adoption/First 90 Days.md` Informative and published at `/adoption/first-90-days`.
- GitHub Release published 2026-09-22T15:48:25Z from the SSH-signed tag on 73d73b9 with the three signed assets. Zenodo version DOI: `10.5281/zenodo.22900969` (concept DOI `10.5281/zenodo.21510450` resolves to it as the latest version), record published 2026-09-22T15:48:29Z.
- `AO-089` to `AO-091` recorded (the judgment share of Core Conformance against `Language/Conformance.md`'s principle; the content-addressing cycle between a record and its evidence; two orders of adoption, closed by a paragraph in `Adoption/First Pilot.md`). `tools/site/site_error_hunt.py`: a local checker over every URL the sitemap names and every internal link they carry (status, title, canonical, robots, JSON-LD, `llms.txt` and `discovery.json` targets); not a CI job, it needs the network.
- Suite: the Reviewer Record. `tools/conformance/validate.py --reviews` reads one named judgment per Test the suite could not decide (Test, Review Pass or Review Fail, reviewer, date, reason), refuses a record it cannot attribute, never overrides a mechanical outcome, and reports the reviewers; `docs/Examples/Conformance/reviewer-record.md` shows the form with a first-party reviewer deciding three Statements. `CAND-028` filed: `docs/Adoption/First 90 Days.md` drafted as a plan with weeks, responsibility slots, artifacts and exit criteria, unpublished until decided.
- Suite, after the acceptance evaluation of 22 September 2026: identity uniqueness and reuse are keyed by the scope and system the Representation Map declares (per type or per collection path), so two systems' keys coexist as `CAND-026` promised; a bare reference to an identity present in two scopes is reported ambiguous; the derived schema makes collections optional and requires a property only where at least two example records carry it. `AO-086` to `AO-088` recorded from the same evaluation. `README.md` no longer says the repository holds no code.
- `CAND-025` (Decided 22 September 2026): accountability is singular and responsibility is plural. One sentence each in `Models/Entity.md` (Ownership) and `Meta/Ownership.md` (Shared Ownership); the Presence Test counts one where a Statement says one, so `REQ-MODELS-ENTITY-004` fails on a second owner; `AO-015` Closed in part.
- `CAND-026` (Decided 22 September 2026): the specification prescribes no format and publishes one Reference Serialization as Informative Adoption material, `docs/Adoption/Reference Serialization.md` (site page `/adoption/reference-serialization`), with `docs/Examples/Conformance/schema.json` derived by `tools/conformance/reference_schema.py` and checked in CI, and the declaration rule for identity scope: the Representation Map's `Identity.scope` row decides `REQ-META-IDENTITY-005`, a Declaration Test.
- `CAND-027` (Decided 22 September 2026): legal form is Organization (Classification; Composition between Organizations) and internal structure is the Department Entity. `Entities/Department/Department.md` replaces the stub Employee and Team already required; one paragraph in `Meta/Organization.md`; one in `Adoption/First Pilot.md`; `CAND-004` postscript for questions 1 and 2.
- `AO-085` closed as a postscript to `CAND-024`: `Memory/Retention.md`'s erasure record names a Policy the organization has declared and the actor who issued it, and an implementation refuses one that names neither; the suite grants the Integrity exclusion only to such a record and reports every erasure record.
- Memory tier, per `CAND-023` and `CAND-024` (both Decided 21 September 2026 as `CAND-007` Section 5 Freeze exceptions, on Reference Case `RC-012`): `Memory/Evidence Overlay.md` carries the Definition, Source, Reliability, Independence and Conformance sections reserved since 21 July 2026; `Memory/Memory Record.md` and `Memory/Evidence Overlay.md` require an implementation to be able to demonstrate that a record is unaltered, naming no mechanism; `Memory/Memory Record.md` defines Audit Record by reference; `Memory/Retention.md`'s Deleted state is an erasure that preserves the record. `Governance/Conformance-Test-Suite.md` gains the Integrity Test kind. Constitution 1.0.1 and Core Vocabulary 0.1 unchanged; `AO-069`, `AO-075`, `AO-084` and `FW-001` closed.

## v1.3.0 (17 September 2026)

- Specification reading path 1.0: the nine chapters of `docs/Specification/` recompiled against the canonical documents as they stand on 17 September 2026. Chapter 1 now carries `Core/Manifest.md`'s sections verbatim, including the whole Normative Language section; Chapters 3 and 4 add Organization to the specializations of Object; Chapter 5 compiles the `CAND-016` specialization sentence and replaces an unsourced sentence about Domains with `Models/Domain.md`'s governance rules; Chapter 6 is restated from `Models/Lifecycle.md`, `Models/State.md`, `Lifecycles/Lifecycles.md`, `Models/Entity.md`, `Models/Domain.md` and `Models/Event.md` and no longer calls the Domains and Entities sections non-normative; Chapter 8's Purpose follows `Language/Conformance.md`; every chapter gains a Revision History and a Last Updated field (GAP-007 closed for the chapters). Constitution 1.0.1 and Core Vocabulary 0.1 unchanged.
- `Specification/Committee Review Package.md` keeps its v0.2 review record and Version, with a dated postscript; the CI version check skips it by name (`CI-DESIGN.md`).
- Root `README.md` badge and text, `docs/README.md`, `PROJECT_STATUS.md` and `Governance/Publication-Model.md` name Specification 1.0.
- GitHub Release published 2026-09-17T09:27:34Z from the SSH-signed tag on 8235b85 with the three signed assets. Zenodo version DOI: `10.5281/zenodo.22809694` (concept DOI `10.5281/zenodo.21510450` resolves to it as the latest version), record published 2026-09-17T09:27:43Z by the GitHub integration on the release event.

## v1.2.0 (16 September 2026)

- Content Release over v1.1.1, cut from commit e4a5a53. Same version tracks (Constitution 1.0.1, Core Vocabulary 0.1 with 13 governed terms, Specification 0.2); the `docs/` tree changes across Governance, Core, Models, Domains, Entities, AI, Language, Adoption, Examples and the Specification reading path.
- `docs/Governance/Master-Architecture-Backlog.md` Part 8, "OCOM Specification v1.0 Ready", is met on the terms recorded there: EPIC-D (Terminology 0.2 as a verbatim index of canonical definitions, GAP-002 closed), EPIC-C (CAND-001 decided and integrated, Domains Memory disposition, every Entity document bridged to Memory), EPIC-B (CAND-016: `Meta/Relationship.md` canonical, `Models/Relationship.md` its specialization; AO-002 closed; CAND-004 dispositioned per question; CAND-015 integrated), EPIC-F (reading path re-pointed to the Constitution, Chapter 4 gains Organization, CAND-002 integrated), EPIC-E (CAND-002 decided; `docs/Governance/Conformance-Test-Suite.md` specifies the Requirement Register as a derived projection, 335 Statements, and five Test kinds) and EPIC-A dispositioned (Layer 1 closes it for v1.0; the World Model document stays gated). The Specification track label stays 0.2.
- Governance registers: AO-062 to AO-068 recorded; AO-001, AO-002, AO-065, AO-066 and OBS-001 closed; AO-042 and AO-062 Open in part; an Entry Lifecycle for observations. Two new Governance documents: `Concept-Paper-Profile-Conformance.md` and `Conformance-Test-Suite.md`.
- Security and project baseline since v1.1.1: `SECURITY.md`, `GOVERNANCE.md` and `CONTRIBUTING.md` per the OpenSSF Baseline; `main` is pull-request-only under a branch ruleset with seven required checks; DCO sign-off and dependency review on every pull request; SSH-signed tags and signed release assets from 13 September 2026. This is the first Release cut with a signed tag and the three signed assets (`OCOM-v1.2.0.zip`, `SHA256SUMS`, `SHA256SUMS.sig`); verification commands are in `SECURITY.md`.
- GitHub Release published 2026-09-16T18:04:46Z. Zenodo version DOI: `10.5281/zenodo.22807178` (concept DOI `10.5281/zenodo.21510450` resolves to it as the latest version), record published 17 September 2026 after the publication-day webhook deliveries failed on Zenodo's side and the release event was redelivered by hand.

## v1.1.1 (12 September 2026)

- Metadata-only patch over v1.1.0. Adds `.zenodo.json` so the Zenodo GitHub integration deposits each Release with the intended metadata (resource type Standard, CC-BY-4.0, ORCID-bound author, subjects, description from the Release body). Narrows the `CITATION.cff` license to the specification text's CC-BY-4.0: Zenodo's citation reader accepts that field only as a single value, and a CFF list means "either licence", which misstates the repository's scoped dual licensing.
- First Release cut per `docs/Governance/Release-Workflow.md`: Manifest entry recorded before tagging, annotated tag. The `docs/` tree is byte-identical to v1.1.0.
- Zenodo version DOI: `10.5281/zenodo.22724309` (concept DOI `10.5281/zenodo.21510450` resolves to it as the latest version).

## v1.1.0 (12 September 2026)

- First Release whose name matches its scope: Constitution 1.0.1, Core Vocabulary 0.1 (13 governed terms), Specification 0.2, the Governance layer (CAND-001 to CAND-015, AO-001 to AO-061), one Reference Case, REUSE compliance, OpenSSF Scorecard and Best Practices, security policy, code of conduct, issue and pull request templates, `CITATION.cff`.
- Superseded by v1.1.1 for citation: the Zenodo deposit for this tag was rejected on the `CITATION.cff` license field, so it carries no version DOI. Tag and release left as published.

## v1.0.0 (23 July 2026)

- First tagged GitHub Release, cut from commit f7a33e2 (22 July 2026) with a lightweight tag. As published it contains the Core Vocabulary 0.1 as it stood then (12 governed terms; Organization was added on 25 July 2026 under CAND-005) and the Specification 0.2 reading path, together with every other tier in `docs/` at that date: Core, Meta, Models, Memory, Lifecycles, Language, AI, Governance, Domains, Entities, Examples, Workflows and Reference Architecture.
- It predates Constitution v1.0 (adopted 26 July 2026) and the Architecture Freeze (CAND-007, 27 July 2026), which is why `docs/Governance/Publication-Manifest.md` records it as Historical: the name does not match the scope. Superseded for citation by v1.1.1.
- Archived in Zenodo as version DOI `10.5281/zenodo.21510451`, resource type Software, under concept DOI `10.5281/zenodo.21510450`.
- No security-relevant changes: the repository was documentation only and carried no CI workflows at that commit. Retitled on 6 September 2026 from "Operational Model" to "Operating Model"; tag, commit and timestamps unchanged.

## v0.1.0

- Initial repository
- Core documentation
- OCOM specification started
